package com.rehab.model.type;

/**
 * Enumeration which stores patient states.
 */
public enum PatientState {

    /**
     * State when patient is at rehabilitation hospital and has active treatments.
     */
    TREATING,

    /**
     * State when patient is not at rehabilitation hospital and does not have active treatments.
     */
    DISCHARGED
}
